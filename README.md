# Monaco WebView2 Loader

Hey guys!

This project is **not originally made by me**, but I'm keeping a copy of the `index.html` here so it doesn't disappear if the original repository gets deleted.

You are free to use the `index.html` from here. I don't plan on deleting this repository unless keeping it becomes illegal or violates any rules.

To use it with WebView2, simply set the source:

```csharp
WebView2.Source = new Uri("https://aixlineyt.github.io/Monaco/index.html");